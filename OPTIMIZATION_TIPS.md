# 🚀 Оптимизация скорости загрузки сайта

## ✅ Что уже сделано:

1. **Preload критических ресурсов** - фоновое изображение и иконки загружаются заранее
2. **Lazy loading изображений** - изображения блюд загружаются только при прокрутке
3. **Оптимизация загрузки фона** - плавное появление фона
4. **Defer для скриптов** - Telegram SDK загружается асинхронно
5. **Preconnect для внешних ресурсов** - ускорение подключения к Telegram

## 📋 Дополнительные рекомендации:

### 1. Оптимизация изображений (ВАЖНО!)

**Сожмите изображения перед загрузкой:**

```bash
# Используйте онлайн-инструменты:
# - TinyPNG (https://tinypng.com/)
# - Squoosh (https://squoosh.app/)
# - ImageOptim (для Mac)

# Рекомендуемые размеры:
# - bg.webp: максимум 1920x1080, качество 80%
# - pit1-6.png: максимум 800x600, качество 85%
# - Иконки (telegram.png, whatsapp.png): максимум 80x80
```

**Конвертируйте PNG в WebP:**
- WebP на 25-35% меньше по размеру
- Сохраняет качество
- Поддерживается всеми современными браузерами

### 2. Используйте CDN для статических ресурсов

Если изображения большие, загрузите их на:
- **Cloudflare CDN** (бесплатно)
- **GitHub Pages** (уже используется)
- **Imgur** или **ImgBB** (для больших изображений)

### 3. Минификация CSS и JS

**Для продакшена:**
- Используйте онлайн-минификаторы CSS/JS
- Или инструменты: `cssnano`, `terser`

### 4. Service Worker для кеширования (опционально)

Создайте файл `docs/sw.js`:
```javascript
const CACHE_NAME = 'growfood-v1';
const urlsToCache = [
  '/',
  '/images/bg.webp',
  '/images/saratov.png',
  '/images/telegram.png',
  '/images/whatsapp.png'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then((cache) => cache.addAll(urlsToCache))
  );
});

self.addEventListener('fetch', (event) => {
  event.respondWith(
    caches.match(event.request)
      .then((response) => response || fetch(event.request))
  );
});
```

И добавьте в `index.html`:
```html
<script>
  if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('/sw.js');
  }
</script>
```

### 5. Оптимизация порядка загрузки

**Приоритет загрузки:**
1. Критические CSS (inline в `<head>`)
2. Критические изображения (preload)
3. JavaScript (defer/async)
4. Остальные изображения (lazy loading)

### 6. Используйте HTTP/2 Server Push

GitHub Pages автоматически использует HTTP/2, что ускоряет загрузку.

### 7. Проверка скорости

**Инструменты для проверки:**
- Google PageSpeed Insights: https://pagespeed.web.dev/
- GTmetrix: https://gtmetrix.com/
- WebPageTest: https://www.webpagetest.org/

**Целевые показатели:**
- First Contentful Paint (FCP): < 1.8s
- Largest Contentful Paint (LCP): < 2.5s
- Time to Interactive (TTI): < 3.8s

## 🎯 Быстрые улучшения (5 минут):

1. **Сожмите bg.webp:**
   - Откройте на https://squoosh.app/
   - Загрузите bg.webp
   - Выберите WebP, качество 80%
   - Сохраните и замените файл

2. **Сожмите изображения блюд (pit1-6.png):**
   - Используйте https://tinypng.com/
   - Загрузите все 6 изображений
   - Замените файлы в `docs/images/`

3. **Проверьте размер файлов:**
   ```bash
   # Каждое изображение должно быть < 200KB
   # bg.webp должен быть < 500KB
   ```

## 📊 Ожидаемые результаты:

После оптимизации:
- ✅ Первая загрузка: **2-3 секунды** (вместо 5-10)
- ✅ Повторная загрузка: **< 1 секунды** (благодаря кешу)
- ✅ Мобильные устройства: **быстрее на 40-60%**

## 🔍 Мониторинг:

После деплоя проверьте:
1. Откройте DevTools (F12)
2. Вкладка "Network"
3. Проверьте время загрузки каждого ресурса
4. Убедитесь, что изображения загружаются с `loading="lazy"`

## 💡 Важно:

**GitHub Pages НЕ является причиной медленной загрузки!**
- GitHub Pages использует CDN
- Загрузка происходит быстро
- Проблема в размере и оптимизации ресурсов

**Основные причины медленной загрузки:**
1. ❌ Большие неоптимизированные изображения
2. ❌ Загрузка всех ресурсов сразу
3. ❌ Отсутствие lazy loading
4. ❌ Нет кеширования

**После оптимизации сайт будет загружаться быстро даже на медленном интернете!**

