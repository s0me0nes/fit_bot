# 🚀 Деплой через GitHub (Render.com)

GitHub сам не хостит Python приложения, но мы можем настроить автоматический деплой через GitHub Actions на Render.com (бесплатный тариф).

## 📋 Что нужно сделать:

### Шаг 1: Создайте аккаунт на Render.com
1. Зайдите на [render.com](https://render.com)
2. Зарегистрируйтесь через GitHub (это важно!)

### Шаг 2: Создайте бэкенд сервис
1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `telegram-bot-backend`
   - **Root Directory**: `backend`
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
   - **Environment Variables**:
     - `PORT` = `8000`
4. Нажмите **"Create Web Service"**
5. Render даст вам URL (например: `https://telegram-bot-backend.onrender.com`)
6. **Скопируйте Service ID** из URL настроек (нужен для GitHub Actions)

### Шаг 3: Создайте бот сервис
1. Нажмите **"New +"** → **"Background Worker"**
2. Подключите тот же GitHub репозиторий
3. Настройки:
   - **Name**: `telegram-bot`
   - **Root Directory**: `bot`
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
   - **Environment Variables**:
     - `BOT_TOKEN` = ваш токен от BotFather
     - `WEB_URL` = URL вашего бэкенда (из шага 2)
4. Нажмите **"Create Background Worker"**
5. **Скопируйте Service ID** из URL настроек

### Шаг 4: Настройте GitHub Secrets (опционально)
Если хотите использовать GitHub Actions для автоматического деплоя:

1. В вашем GitHub репозитории: **Settings** → **Secrets and variables** → **Actions**
2. Добавьте секреты:
   - `RENDER_API_KEY` - получите в Render: Account Settings → API Keys
   - `RENDER_BACKEND_SERVICE_ID` - ID сервиса бэкенда
   - `RENDER_BOT_SERVICE_ID` - ID сервиса бота

### Шаг 5: Push в GitHub
```bash
git add .
git commit -m "Initial commit"
git push origin main
```

Render автоматически задеплоит оба сервиса при push в GitHub! 🎉

---

## ⚠️ Важные моменты:

### Минусы Render.com:
1. **Бесплатный тариф "засыпает"** после 15 минут неактивности
   - Первый запрос после простоя может занять 30-60 секунд
   - Для бота это не критично, но для веб-сайта заметно

2. **Ограничения бесплатного тарифа:**
   - 750 часов в месяц (достаточно для 24/7 работы)
   - Медленный старт после простоя
   - Нет гарантированного SLA

### Альтернативы с бесплатным тарифом:

1. **Fly.io** - не засыпает, но сложнее настройка
2. **Railway** - проще, но платный после бесплатного периода
3. **VPS** (Hetzner ~3€/мес) - полный контроль, не засыпает

---

## 🔄 Автоматический деплой

После настройки, каждый раз когда вы делаете `git push`, Render автоматически:
- Обнаружит изменения
- Пересоберет приложение
- Задеплоит новую версию

Никаких дополнительных действий не требуется!

---

## ✅ Проверка работы

1. **Бэкенд**: Откройте URL в браузере
2. **Бот**: Откройте бота в Telegram, отправьте `/start`

---

## 🐛 Решение проблем

### Сервис не запускается
- Проверьте логи в Render Dashboard
- Убедитесь, что все зависимости в `requirements.txt`
- Проверьте переменные окружения

### Медленный ответ
- Это нормально для бесплатного тарифа после простоя
- Первый запрос всегда медленный
- Последующие запросы быстрые

### Бот не отвечает
- Проверьте логи бота в Render
- Убедитесь, что `BOT_TOKEN` правильный
- Проверьте, что `WEB_URL` указывает на правильный URL бэкенда

