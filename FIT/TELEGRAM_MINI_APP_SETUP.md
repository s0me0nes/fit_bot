# 🤖 Настройка Telegram Mini App

## Что такое Mini App?

**Telegram Mini App (Web App)** - это веб-приложение, которое открывается прямо внутри Telegram. Пользователи видят его как полноценное приложение внутри бота.

### ✅ Преимущества:
- Открывается внутри Telegram (не нужно переходить в браузер)
- Доступ к данным пользователя (имя, фото, ID)
- Нативные кнопки Telegram
- Красивый интерфейс
- Работает на всех устройствах

---

## 📋 Пошаговая настройка:

### Шаг 1: Настройте бота

Бот уже настроен! В файле `bot/main.py` добавлена кнопка для открытия Mini App.

### Шаг 2: Загрузите сайт на GitHub Pages

1. **Загрузите файлы на GitHub:**
   ```bash
   git add .
   git commit -m "Add Mini App support"
   git push
   ```

2. **Включите GitHub Pages:**
   - Settings → Pages
   - Source: `main` / `docs`
   - Сохраните

3. **Скопируйте URL вашего сайта:**
   ```
   https://ваш-username.github.io/FIT/
   ```

### Шаг 3: Настройте переменную окружения

В файле `.env` (или в настройках хостинга бота) укажите:

```env
WEB_URL=https://ваш-username.github.io/FIT/
```

**Важно:** URL должен быть **HTTPS** (GitHub Pages автоматически дает HTTPS).

### Шаг 4: Запустите бота

```bash
cd bot
python main.py
```

### Шаг 5: Протестируйте

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "🍽️ Открыть меню"
4. Mini App откроется внутри Telegram!

---

## 🎨 Что уже настроено:

### В боте (`bot/main.py`):
- ✅ Кнопка для открытия Mini App
- ✅ Команда `/start` с кнопкой
- ✅ Команда `/menu` для открытия меню
- ✅ Команда `/help` с кнопкой

### В веб-приложении (`docs/index.html`):
- ✅ Telegram Web App SDK подключен
- ✅ Автоматическое расширение на весь экран
- ✅ Кнопка "Назад" в Telegram
- ✅ Настройка цветов
- ✅ Обработка кликов на товары

---

## 🔧 Дополнительные возможности:

### Получить данные пользователя:

```javascript
const tg = window.Telegram.WebApp;
const user = tg.initDataUnsafe?.user;

if (user) {
    console.log('Имя:', user.first_name);
    console.log('Фамилия:', user.last_name);
    console.log('Username:', user.username);
    console.log('ID:', user.id);
}
```

### Отправить данные обратно в бот:

```javascript
// Показать уведомление
tg.showAlert('Сообщение для пользователя');

// Отправить данные в бот
tg.sendData(JSON.stringify({
    action: 'order',
    item: 'Блюдо 1',
    price: 500
}));
```

### Обработать данные в боте:

В `bot/main.py` добавьте обработчик:

```python
from telegram.ext import CallbackQueryHandler

async def handle_web_app_data(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработка данных из Mini App"""
    data = update.effective_message.web_app_data.data
    # data - это JSON строка
    import json
    order = json.loads(data)
    await update.message.reply_text(f"Получен заказ: {order}")
```

---

## 🎯 Примеры использования:

### Для ресторана:
- Меню блюд
- Заказ через бота
- Информация о блюдах
- Фото блюд

### Для магазина:
- Каталог товаров
- Корзина покупок
- Оформление заказа
- История заказов

### Для услуг:
- Список услуг
- Бронирование
- Оплата
- Уведомления

---

## ⚠️ Важные моменты:

1. **HTTPS обязателен!** Mini App работает только по HTTPS
   - GitHub Pages автоматически дает HTTPS ✅
   - Локальный тест: используйте ngrok или другой туннель

2. **Домен должен быть валидным:**
   - GitHub Pages: `*.github.io` ✅
   - Свой домен: нужно настроить в BotFather

3. **Настройка домена в BotFather (опционально):**
   - Откройте [@BotFather](https://t.me/BotFather)
   - `/setdomain`
   - Выберите бота
   - Укажите домен (например: `your-username.github.io`)

---

## 🐛 Решение проблем:

### Mini App не открывается:
- Проверьте, что URL начинается с `https://`
- Убедитесь, что сайт доступен в браузере
- Проверьте переменную `WEB_URL` в боте

### Кнопка не появляется:
- Убедитесь, что бот обновлен
- Перезапустите бота
- Проверьте, что используется последняя версия `python-telegram-bot`

### Данные пользователя не доступны:
- Mini App должен открываться из Telegram
- В браузере данные недоступны (это нормально)

---

## ✅ Готово!

Теперь у вас есть:
- ✅ Telegram бот с кнопкой Mini App
- ✅ Веб-приложение на GitHub Pages
- ✅ Интеграция с Telegram Web App SDK
- ✅ Полностью бесплатно и не засыпает!

**Просто загрузите на GitHub и запустите бота!**

