# 🔧 Решение проблем с ботом

## Проблема: Бот не отвечает на команду /start

### ✅ Проверка 1: Бот подключен к Telegram API

Запустите тестовый скрипт:
```bash
python bot/test_bot.py
```

Если видите "✅ Бот успешно подключен!" - токен правильный.

### ✅ Проверка 2: Бот запущен

**Важно:** Бот должен быть запущен постоянно, чтобы получать сообщения!

#### Локальный запуск:

1. Откройте терминал
2. Перейдите в папку проекта:
   ```bash
   cd C:\Users\Leon\Desktop\FIT
   ```
3. Запустите бота:
   ```bash
   python bot/main.py
   ```

Вы должны увидеть:
```
🚀 Запуск телеграм-бота...
BOT_TOKEN установлен: ✅ Да
✅ Бот успешно подключен: @fit_eda_bot
Бот готов к работе и ожидает команды...
```

**⚠️ ВАЖНО:** Оставьте терминал открытым! Если закрыть терминал, бот остановится.

#### Проверка, запущен ли бот:

В Windows PowerShell:
```powershell
Get-Process python | Where-Object {$_.Path -like "*python*"}
```

Или проверьте, есть ли процесс `python bot/main.py` в диспетчере задач.

### ✅ Проверка 3: Логи бота

Когда бот запущен и вы отправляете `/start`, в терминале должны появиться логи:

```
📨 Получена команда /start от пользователя:
   ID: 123456789
   Имя: Ваше Имя
📤 Отправка ответа пользователю...
✅ Ответ на /start отправлен успешно
```

Если этих логов нет - бот не получает обновления.

### ✅ Проверка 4: Правильный бот

Убедитесь, что вы пишете правильному боту:
- Username: `@fit_eda_bot`
- Имя: `fit-eda`

### ✅ Проверка 5: Бот развернут на сервере?

Если бот развернут на Railway/Render/Fly.io:

1. Проверьте логи в панели управления сервисом
2. Убедитесь, что сервис запущен (не в режиме "sleeping")
3. Проверьте переменные окружения:
   - `BOT_TOKEN` установлен
   - `WEB_URL` установлен

## 🚀 Быстрое решение

### Вариант 1: Запустить локально

```bash
# 1. Откройте терминал
# 2. Перейдите в папку проекта
cd C:\Users\Leon\Desktop\FIT

# 3. Запустите бота
python bot/main.py

# 4. Оставьте терминал открытым
# 5. Попробуйте отправить /start боту
```

### Вариант 2: Запустить в фоне (Windows)

Создайте файл `start_bot.bat`:
```batch
@echo off
cd /d C:\Users\Leon\Desktop\FIT
start "Telegram Bot" python bot/main.py
```

Дважды кликните на файл - бот запустится в отдельном окне.

### Вариант 3: Использовать systemd (Linux) или службы Windows

Для постоянной работы бота на сервере.

## 📋 Чек-лист диагностики

- [ ] Бот подключен к API (запустите `python bot/test_bot.py`)
- [ ] Бот запущен (`python bot/main.py` работает)
- [ ] Терминал/окно с ботом открыто
- [ ] Видны логи при отправке `/start`
- [ ] Правильный бот в Telegram (`@fit_eda_bot`)
- [ ] Интернет-соединение работает

## 🔍 Частые ошибки

### Ошибка: "BOT_TOKEN не установлен"
**Решение:** Создайте файл `.env` в корне проекта:
```
BOT_TOKEN=ваш_токен_здесь
WEB_URL=https://your-username.github.io/FIT/
```

### Ошибка: "Connection refused" или "Timeout"
**Решение:** 
- Проверьте интернет-соединение
- Проверьте, не блокирует ли файрвол Telegram API
- Попробуйте позже (Telegram API может быть временно недоступен)

### Ошибка: Бот отвечает, но медленно
**Решение:**
- Это нормально для бесплатных хостингов (Railway, Render)
- Используйте платный хостинг или VPS для быстрого отклика

## 💡 Рекомендации

1. **Для разработки:** Запускайте бота локально
2. **Для продакшена:** Используйте Railway, Render или VPS
3. **Мониторинг:** Следите за логами бота
4. **Автозапуск:** Настройте автозапуск при перезагрузке сервера

## 📞 Если ничего не помогает

1. Проверьте логи бота (должны быть подробные сообщения)
2. Убедитесь, что используете последнюю версию кода
3. Перезапустите бота
4. Проверьте, не заблокирован ли бот в Telegram

